{
  "install": "docker compose pull",
  "start": "sudo service docker start 2>/dev/null || true; docker compose up -d",
  "terminals": [
    {
      "name": "wordpress",
      "command": "docker compose logs -f wordpress"
    }
  ]
}
